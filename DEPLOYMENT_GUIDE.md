# 🌐 Webアプリ化＆オンライン無料公開ガイド

このアプリは、データベースや特別なサーバー設定を必要としない**完全な静的Webアプリケーション（HTML/CSS/JS/JSON）**です。
以下の方法で、**完全無料**で数分でオンライン公開でき、スマホやiPadからいつでもアクセス・学習できるようになります。

---

## 🚀 公開方法1：Vercel（★最もおすすめ・数秒で完了）

Vercelは静的Webアプリの最も人気な無料ホスティングサービスです。

### 手順
1. **GitHubにコードをプッシュ**（またはVercel CLIを使用）。
2. [Vercel公式サイト](https://vercel.com) にログイン。
3. 「Add New Project」から本リポジトリを選択。
4. 「Deploy」ボタンを押すだけで、数秒で専用URL（例: `https://med-exam-app.vercel.app`）が発行されます。

---

## 🚀 公開方法2：GitHub Pages（完全無料）

1. 本プロジェクトを GitHub リポジトリ（Public）として作成・プッシュ。
2. GitHubの「Settings」タブ ➔ 左メニュー「Pages」を開く。
3. **Build and deployment** の Source で `Deploy from a branch` を選択。
4. Branch を `main` / `root` に設定して「Save」。
5. 数分後に `https://<ユーザー名>.github.io/med-exam-app` でアクセス可能になります。

---

## 📱 スマホ・タブレットでアプリ化（PWAインストール）する方法

Webアプリとして公開されたURL（またはローカルサーバー）をスマホで開くと、PWA機能によりホーム画面にアプリとして登録できます。

### iPhone / iPad (Safari) の場合
1. SafariでアプリのURLを開く。
2. 画面下部（iPadは上部）の **共有ボタン（四角と上矢印のアイコン）** をタップ。
3. **「ホーム画面に追加」** を選択。
4. ホーム画面に「国試演習」のアイコンが追加され、オフラインでもアプリとして起動できます！

### Android (Chrome) の場合
1. ChromeでアプリのURLを開く。
2. 画面右上の **メニューボタン（縦の3点リーダー）** をタップ。
3. **「アプリをインストール」** または **「ホーム画面に追加」** を選択。
4. ホーム画面からネイティブアプリと同じ感覚で利用可能になります。
